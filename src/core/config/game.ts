// Game configuration constants
export const GAME_CONFIG = {
  tickRate: 100, // milliseconds
  startingBiomass: 0,
  startingClickPower: 1,
  maxBlobSize: 400,
  minBlobSize: 50,
  blobSizeMultiplier: 10,
  maxParticles: 100,
  particleSpawnRate: 2, // particles per second
  nutrientSpawnThreshold: 20, // spawn more when below this count
  nutrientSpawnCount: 10, // how many to spawn at once
  gameAreaWidth: 2000,
  gameAreaHeight: 1500,
}; 